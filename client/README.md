Client (Vite + React)

Install and run:

```powershell
cd client
npm install
npm run dev
```

Open the dev server at the printed URL (usually http://localhost:5173) and ensure the server API is running at `http://localhost:4000`.
